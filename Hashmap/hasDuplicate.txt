class Solution {
    public boolean hasDuplicate(int[] nums) {
        MapInteger,Integerm1=new HashMap();
        for(int inums){
            m1.put(i,m1.getOrDefault(i,0)+1);
        }
        for(int inums){
            
            if (m1.get(i)1)
                return true;
        }
        return false;
    }
}